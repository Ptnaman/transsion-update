## Setup

1. Add GitHub Actions secrets `BOT_TOKEN` and `CHAT_ID`.
2. Optionally add `TELEGRAPH_TOKEN`.
3. Run the `Transsion OTA Telegram Monitor` workflow manually once if you want to test it.
4. Scheduled checks run every 30 minutes.
